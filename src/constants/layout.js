/** Matches AppHeaderShell — header stays full shell width. */
export const APP_SHELL_MAX_WIDTH = '1600px'

/** Narrower main column for dashboard / builder workspace. */
export const APP_CONTENT_MAX_WIDTH = '1200px'

/** Reserved space on each side for future shortcuts (xl+). */
export const APP_SIDE_RAIL_WIDTH = '4rem'
